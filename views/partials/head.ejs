<header>
  <div class="main-header">

    <% if (showAppName) { %>
      <div class="app-name">Welcome to FlexiFitApp!</div>
    <% } %>

    <% if (showApp) { %>
      <div class="app-name">TRAIN HARD.</div>
      <div class="app-name">HAVE FUN.</div>
      <div class="app-name">MAKE FRIENDS.</div>
    <% } %>

    


  </div>
</header>